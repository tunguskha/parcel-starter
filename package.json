{
  "name": "parcel-starter",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "npx parcel dev/*.html -d dev-parcel --open --no-cache",
    "build": "npx parcel build dev/*.html -d prod --public-url ./dist/ --no-source-maps --no-cache"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "debug-css": "^1.0.0",
    "parcel-bundler": "^1.12.4",
    "sass": "^1.23.0"
  }
}